function addTwoNumbers(num1, num2) {
  return num1 + num2;
}

var answer = addTwoNumbers(5, 2);

console.log('addTwoNumbers exercise:', answer);

function convertHoursToMinutes(hours) {
  return hours * 60;
}

var convertedHours = convertHoursToMinutes(5);

console.log('convertHoursToMinutes exercise:', convertedHours);

function getGreeting(name) {
  return 'Hi ' + name + '!';
}

var greeting = getGreeting('Obi');

console.log('getGreeting exercise:', greeting);

function addAndMultiplyBy5(num1, num2) {
  return (num1 + num2) * 5;
}

var addAndMultiplyBy5Result = addAndMultiplyBy5(129, 736);

console.log('addAndMultiplyBy5 exercise:', addAndMultiplyBy5Result);

function subtractTwoNumbers(num1, num2) {
  return num1 - num2;
}

var subtractTwoNumbersResult = subtractTwoNumbers(87, 12);

console.log('subtractTwoNumbers exercise:', subtractTwoNumbersResult);

function getCircleCircumference(radius) {
  return 2 * Math.PI * radius;
}

var getCircleCircumferenceResult = getCircleCircumference(5);

console.log('getCircleCircumference exercise:', getCircleCircumferenceResult);

function getFullName(firstName, lastName) {
  return firstName + ' ' + lastName;
}

var getFullNameResult = getFullName('Michael', 'Jordan');

console.log('getFullName exercise:', getFullNameResult);

function cube(number) {
  return Math.pow(7, 3);
}

var cubeResult = cube(3);

console.log('cube exercise:', cubeResult);
